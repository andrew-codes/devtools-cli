[mergetool "diffmerge"]
	keepBackup = false
	trustExitCode = false
	cmd = diffmerge -merge -result="$PWD/$MERGED" "$PWD/$LOCAL" "$PWD/$BASE" "$PWD/$REMOTE"

[difftool "diffmerge"]
 	cmd = cmd = diffmerge "$LOCAL" "$REMOTE"